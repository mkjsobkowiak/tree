CREATE TABLE NODE (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
  VALUE DOUBLE,
  PARENT_ID BIGINT,
  CONSTRAINT fk_parent_id_node FOREIGN KEY (PARENT_ID) REFERENCES NODE(ID),
);

INSERT INTO NODE (ID, VALUE, PARENT_ID) VALUES (null, null, null);